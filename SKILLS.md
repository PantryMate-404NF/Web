# AI Pantry Web — 팀 작업 흐름

## 화면 구현

1. 화면 요구사항과 정상·로딩·빈·오류·미인증 상태를 확인합니다.
2. 390 × 844에서 모바일 우선으로 구현한 뒤 360px와 430px 폭을 확인합니다.
3. 라벨, 포커스, 터치 영역, 텍스트 기반 상태 표현을 구현 과정에서 함께 적용합니다.
4. UI 변경은 PR에 스크린샷 또는 짧은 녹화를 첨부합니다.

## REST API와 MSW

1. 화면 구현 전 REST API 계약을 합의합니다.
2. 정상·빈·오류 응답에 대응하는 MSW 핸들러를 작성합니다.
3. 프론트엔드는 먼저 MSW 환경에서 구현합니다.
4. 실제 API가 준비되면 문서화되지 않은 응답에 UI를 맞추지 말고 계약을 검증합니다.

## 성능과 접근성

- 비교 가능한 화면은 일관된 Chrome 환경의 390 × 844에서 측정합니다.
- Lighthouse의 LCP·CLS를 기록하고, 실제 사용자 INP 대신 Lighthouse 수치를 사용하지 않도록 핵심 인터랙션은 DevTools Performance에서 확인합니다.
- 팬트리 상태를 색상만으로 전달하지 않습니다.
- 아이콘 전용 컨트롤에는 접근 가능한 이름을, 정보성 이미지에는 유용한 대체 텍스트를 제공합니다.
